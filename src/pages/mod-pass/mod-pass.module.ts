import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { ModPassPage } from './mod-pass';

@NgModule({
  declarations: [
    ModPassPage,
  ],
  imports: [
    IonicPageModule.forChild(ModPassPage),
  ],
})
export class ModPassPageModule {}
